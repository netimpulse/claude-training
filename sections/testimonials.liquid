<section class="tm color-scheme color-{{ section.settings.color_scheme.id }}" data-section-id="{{ section.id }}">
  {%- if section.settings.heading != blank -%}<h2 class="tm__heading">{{ section.settings.heading | escape }}</h2>{%- endif -%}
  <div class="tm__grid" style="--cols: {{ section.settings.columns }};">
    {%- for block in section.blocks -%}
      <figure class="tm__card" {{ block.shopify_attributes }}>
        {%- if block.settings.rating > 0 -%}
          <div class="tm__stars" aria-label="{{ block.settings.rating }} von 5 Sternen">
            {%- for i in (1..5) -%}
              {%- if i <= block.settings.rating -%}★{%- else -%}☆{%- endif -%}
            {%- endfor -%}
          </div>
        {%- endif -%}
        <blockquote class="tm__quote">{{ block.settings.quote }}</blockquote>
        <figcaption class="tm__author">
          <strong>{{ block.settings.author | escape }}</strong>
          {%- if block.settings.meta != blank -%}<span>{{ block.settings.meta | escape }}</span>{%- endif -%}
        </figcaption>
      </figure>
    {%- endfor -%}
  </div>
</section>
{% style %}
  {%- assign scheme = section.settings.color_scheme -%}
  {%- if scheme -%}
  .color-{{ scheme.id }} {
    --color-background: {{ scheme.settings.background.red }}, {{ scheme.settings.background.green }}, {{ scheme.settings.background.blue }};
    --color-text: {{ scheme.settings.text.red }}, {{ scheme.settings.text.green }}, {{ scheme.settings.text.blue }};
    --color-button: {{ scheme.settings.button.red }}, {{ scheme.settings.button.green }}, {{ scheme.settings.button.blue }};
    --color-button-label: {{ scheme.settings.button_label.red }}, {{ scheme.settings.button_label.green }}, {{ scheme.settings.button_label.blue }};
    --color-secondary-button-label: {{ scheme.settings.secondary_button_label.red }}, {{ scheme.settings.secondary_button_label.green }}, {{ scheme.settings.secondary_button_label.blue }};
    --color-shadow: {{ scheme.settings.shadow.red }}, {{ scheme.settings.shadow.green }}, {{ scheme.settings.shadow.blue }};
    color: rgb(var(--color-text));
    background-color: rgb(var(--color-background));
    {%- if scheme.settings.background_gradient != blank -%}
    background: {{ scheme.settings.background_gradient }};
    {%- endif -%}
  }
  {%- endif -%}

  #shopify-section-{{ section.id }} {
    --tm-bg: {{ section.settings.bg_color }};
    --tm-fg: {{ section.settings.text_color }};
    --tm-padding-top: {{ section.settings.padding_top }}px;
    --tm-padding-bottom: {{ section.settings.padding_bottom }}px;
  }
{% endstyle %}
{{ 'section-testimonials.css' | asset_url | stylesheet_tag }}
{% schema %}
{
  "name": "t:sections.testimonials.name_testimonials",
  "tag": "section",
  "class": "section-testimonials",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.testimonials.label_farbschema",
      "default": "scheme-1"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "t:sections.testimonials.label_ueberschrift",
      "default": "t:sections.testimonials.default_was_unsere_kund_innen_sagen"
    },
    {
      "type": "range",
      "id": "columns",
      "min": 1,
      "max": 4,
      "step": 1,
      "label": "t:sections.testimonials.label_spalten_desktop",
      "default": 3
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "t:sections.testimonials.label_hintergrund",
      "default": "#f6f5f2"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "t:sections.testimonials.label_text",
      "default": "#111111"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "t:sections.testimonials.label_abstand_oben",
      "default": 64
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "t:sections.testimonials.label_abstand_unten",
      "default": 64
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "t:sections.testimonials_blocks.name_testimonial",
      "limit": 9,
      "settings": [
        {
          "type": "richtext",
          "id": "quote",
          "label": "t:sections.testimonials.label_zitat",
          "default": "t:sections.testimonials.default_p_fantastische_qualitaet_sitzt_perfekt_u"
        },
        {
          "type": "text",
          "id": "author",
          "label": "t:sections.testimonials.label_autor_in",
          "default": "t:sections.testimonials.default_lena_k"
        },
        {
          "type": "text",
          "id": "meta",
          "label": "t:sections.testimonials.label_meta_z_b_produkt",
          "default": "t:sections.testimonials.default_verifizierte_kaeuferin"
        },
        {
          "type": "range",
          "id": "rating",
          "min": 0,
          "max": 5,
          "step": 1,
          "label": "t:sections.testimonials.label_sterne",
          "default": 5
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "t:sections.testimonials.preset_testimonials",
      "blocks": [
        {
          "type": "testimonial"
        },
        {
          "type": "testimonial"
        },
        {
          "type": "testimonial"
        }
      ],
      "settings": {
        "color_scheme": "scheme-1"
      }
    }
  ],
  "disabled_on": {
    "groups": [
      "header",
      "footer"
    ]
  }
}
{% endschema %}
